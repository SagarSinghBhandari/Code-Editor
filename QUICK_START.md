# Quick Start Guide

## One-Command Installation

```bash
npm install
```

That's it! This single command will install all required dependencies.

## What Gets Installed

### Core Dependencies (12 packages)
- React & React DOM
- React Router (navigation)
- Monaco Editor (code editor)
- Supabase (authentication)
- Axios (API calls)
- Recharts (charts/graphs)
- React Icons
- Tailwind CSS dependencies

### Development Tools (11 packages)
- Vite (build tool)
- ESLint (code quality)
- Tailwind CSS & PostCSS
- TypeScript types

## After Installation

1. **Start the development server:**
   ```bash
   npm run dev
   ```

2. **Open your browser:**
   Navigate to the URL shown in the terminal (usually `http://localhost:5173`)

3. **You're ready to code!**

## Package Summary

**Total Packages:** 23
- **Production:** 12 packages
- **Development:** 11 packages

**Installation Time:** ~1-2 minutes (depending on internet speed)

## Need Help?

If installation fails:
1. Make sure Node.js is installed: `node --version`
2. Clear cache: `npm cache clean --force`
3. Delete `node_modules` and reinstall: `rm -rf node_modules && npm install`

